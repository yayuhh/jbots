{
  "name": "jbots",
  "version": "0.1.0",
  "description": "slack bot boilerplate via Hapi",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines":{
    "node": "0.10.x"
  },
  "author": "johnny domino",
  "license": "BSD-2-Clause",
  "dependencies": {
    "hapi": "^2.6.0"
  }
}
